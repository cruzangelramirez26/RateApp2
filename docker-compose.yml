version: '3.8'

services:
  app:
    build: .
    ports:
      - "${PORT:-8000}:8000"
    env_file:
      - .env
    environment:
      - FRONTEND_URL=http://localhost:8000
    restart: unless-stopped
